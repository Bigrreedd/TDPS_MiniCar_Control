# TDPS Day 1 Road Test - Baseline Validation
**Date:** 2026-06-04  
**Branch:** `LHX/upper-test` (今天 bench 调好的版本)  
**Mode:** Bench test mode (不依赖里程门控状态机)

---

## 测试目标

验证今天 bench 调好的 PID 在实车硬件上是否稳定，为 Day 2 全图测试打基础。

---

## 硬件准备清单

- [ ] 电池：3S 11.1V 850mAh 75C，满电（>11.1V）
- [ ] 卷尺：100cm 标定距离用
- [ ] 笔记本：串口监视器（USART3）
- [ ] ST-Link：现场刷固件用
- [ ] 备用电池：至少 1 块
- [ ] 万用表：测电压
- [ ] 螺丝刀：调传感器

---

## 固件配置（当前 upper-test）

```c
// 已设置好，无需改动
BENCH_FIXED_SPEED_ENABLE = 1        // 保持 bench 模式
BENCH_FIXED_TARGET_CPS = 50.0f      // 目标 50
SENSOR_COUNT = 6                    // 6 路灰度
USART3_DEBUG_ON_PB10 = 1           // 串口调试开
```

**速度 PID（已调好）：**
- 50 cnt/s：Kp=1.25, Ki=3.42, Kd=1.25
- 死区：start=900, hold=820

**位置 PID（已调好）：**
- Kp=60, Ki=0, Kd=80
- Bench correction limit=30

---

## Phase 1：编码器标定（30 分钟）

### 测试步骤

1. **准备：**
   - 在平整地面用卷尺量出准确 100cm 直线
   - 车头对齐起点标记

2. **运行：**
   - K1 启动（50 cnt/s）
   - 手动停在 100cm 终点标记

3. **记录：**
   - 串口读取最终里程值：`Mil=XXX`
   - 计算：`实际 TICKS_PER_CM = 总 ticks / 100`

### 成功标准

- 实际里程 = 100cm ± 5cm
- 如果偏差 >5cm，记录实际值，晚上修正 `ENCODER_TICKS_PER_CM`

### 失败处理

| 偏差 | 原因 | 处理 |
|---|---|---|
| >10cm | 编码器接线松动 | 检查接线 |
| 左右轮差 >5cm | 轮子直径不同 | 记录，晚上补偿 |
| 里程不增长 | 编码器无信号 | 检查下板协议 |

---

## Phase 2：直线稳定性测试（30 分钟）

### 测试步骤

1. **50 cnt/s 测试：**
   - 2-3 米直线黑线
   - K1 启动，跑 10 秒
   - 观察：车是否直线行驶，速度是否稳定

2. **60 cnt/s 测试：**
   - 修改固件：`BENCH_FIXED_TARGET_CPS = 60.0f`
   - 重新刷入
   - 重复上述测试

### 成功标准

- 速度波动 < ±5 cnt/s
- 车身偏离中线 < ±3cm
- 无明显震荡
- 电池 > 6.5V

### 遥测记录

串口每 300ms 输出：
```text
L=XX R=XX T=XX out=XX pid=XX,XX sent=XXX,XXX S=...
```

重点看：
- `L/R` 是否围绕目标 ±5
- `pid` 左右差是否 <20（应该接近相等）
- `sent` 是否在 820~900 范围

### 失败处理

| 现象 | 原因 | 调整 |
|---|---|---|
| 速度抖动 ±10 | Kp 太大 | 现场降 Kp：15→10 |
| 起步慢 >2s | Ki 太小 | 现场升 Ki：5→8 |
| 左右速度差 >5 | 编码器极性 | 检查 TIM2/TIM4 |
| 偏离中线 >5cm | 位置环失效 | 检查传感器 |

---

## Phase 3：手动 S 弯测试（45 分钟）

**重要：** 不依赖里程自动切段，手动把车放在 S 弯起点。

### 测试步骤

1. **准备：**
   - 找到赛道 S 弯段（~150cm 位置）
   - 手动把车放在 S 弯入口

2. **运行：**
   - K1 启动（50 cnt/s）
   - 观察车是否能跟随 S 弯

3. **记录：**
   - 是否丢线
   - 是否震荡
   - `pid` 左右差变化

### 成功标准

- 完整跟随 S 弯，不丢线
- 左右差速明显（`pid` 差 40~60）
- 无过冲、无震荡

### 失败处理

| 现象 | 原因 | 调整 |
|---|---|---|
| 丢线 | 速度太快 | 降到 40 cnt/s |
| 震荡 | Kp 太大 | 降 Kp：60→50 |
| 过冲 | Kd 太小 | 升 Kd：80→100 |
| 转向不足 | 位置 correction 被 cap 压 | 现场看，晚上改 |

---

## Phase 4（可选）：启停测试（20 分钟）

如果前面顺利，测试启停可靠性。

### 测试步骤

1. K1 启动 → 跑 2 秒 → K2 停止
2. 重复 5 次
3. 观察：每次启动是否干净，停止是否立即

### 成功标准

- 启动 <300ms 轮子转动
- 停止后无蠕动
- 编码器计数停止时归零或冻结

---

## 测试结束清单

### 记录数据

- [ ] 编码器标定值：`实际 TICKS_PER_CM = ___`
- [ ] 50 cnt/s 速度波动范围：`L=___ ~ ___, R=___ ~ ___`
- [ ] 60 cnt/s 速度波动范围：`L=___ ~ ___, R=___ ~ ___`
- [ ] S 弯是否通过：[ ] 是 [ ] 否
- [ ] 位置环差速是否明显：[ ] 是 [ ] 否
- [ ] 电池终止电压：`___ V`

### 问题记录

列出所有失败项和现象：
```text
1. 
2. 
3. 
```

### 带回实验室

- [ ] 遥测日志完整保存
- [ ] 问题清单整理
- [ ] 电池充电

---

## Day 1 晚上任务（实验室）

基于 Day 1 测试结果：

1. **提交今天改动**
2. **合并 upper-test → competition**
3. **修正里程门控**（用正确地图距离）
4. **修复速度单位转换**
5. **根据 Day 1 问题调整 PID**
6. **如果编码器标定值不对，更新常量**

---

## 预期结果

**最低目标（必须达成）：**
- ✅ 编码器标定完成
- ✅ 直线 50 稳定
- ✅ 启停可靠

**理想目标（尽力）：**
- ✅ 直线 60 也稳定
- ✅ S 弯能跟
- ✅ 位置环差速明显

**Day 2 条件：**
- 最低目标达成 → Day 2 可以测全图
- 理想目标达成 → Day 2 直接冲完整跑

---

## 安全注意

- 电池 <6.0V 立即停止
- 车偏离 >15° 手动停
- 冲出赛道立即断电
- 每 5 次测试检查一次螺丝

---

**测试愉快！Day 1 重点是验证基础，不求快，求稳。**
