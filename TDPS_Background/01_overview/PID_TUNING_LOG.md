# PID 调参日志 v2 —— 新硬件时代(2026-06-07 中午起)

> **开档**:2026-06-07 13:57。
> **旧日志封档**:`archives/PID_TUNING_LOG_v1_F1-F23_20260605-0607.md`(7340 行,覆盖 2026-06-05 ~ 06-07 上午,F1~F23 全部轮次)。
> **封档原因**:①硬件结构 + 地面平整度修改优化,旧标定的物理前提已变,旧结论整体降级为"先验"(引用须注明 v1);②7340 行已超出单文件可维护范围,自然断代点轮换。
> **历史串口归档**(`serial_raw_*.txt`)仍在本目录,不随封档移动,v1 内引用路径不变。

---

## 时代分界声明(2026-06-07)

- **硬件结构**:进行了修改优化(⚠具体改动项待用户补充:配重/传感器高度/轮系/电池座/其他——记入下方 R1 条目)。
- **地面**:平整度优化。v1 旧账中两类地形病的前提可能已消失:
  - 机械托底(双轮 el/er 冻结 + yw 恒值,v1 01:50/10:59 两次复发);
  - 赛道褶皱处 START 拉不动(v1 D1 把 START 820/940→870/990 的原始动机)。
- **影响评估**:
  - 摩擦域参数(START/HOLD/boost 帽)——标定前提直接变化,旧值存疑;
  - 回路域参数(Kp/Kd/α)——植物增益(抓地/惯量/几何)可能平移,全史最优核仍是最佳先验;
  - 里程刻度(2.4cm/cnt)——若轮系/编码器被动过则失效,**须 R0 复验**。

## 用户红线(2026-06-07,本时代有效)

1. **占空比类参数先不改**(用户原话"这里的占空比先不改"):`START 870/990`、`HOLD 850/940`、`S_MODE_HOLD 730/760`、boost 发车帽——全部冻结,解冻须用户拍板。
2. 每轮测试必录日志:串口全量 + 代码改动 + 串口对应代码版本 + 表现判读 + 硬件条件 + 困难 + 用户全部原话(沿用 v1 纪律)。
3. **每次改完代码都 commit + push GitHub**(用户本日重申)。
4. 继承 v1 红线:Ki=0 不动;单变量轮次纪律;手测电池电压每轮硬性(v1 连欠 6 轮的教训)。

---

## 继承基线 = F23(commit `3a06988`,新时代 R1 固件,待烧录)

F23 = 全史复盘回滚轮:控制核回到 03:30 史上最远 + 19:49 蛇形①全通 + 06-05 直线判定通过的同一组参数。**F23 落码后尚未跑过车**——R1 既是新硬件基准轮,也是 F23 的首验轮。

| 域 | 参数 | R1 值 | 状态 | 新实测扫描区间 |
|---|---|---|---|---|
| 位置环 | Kp | **40** | 起点(全史最优核) | [32, 48],步 ±6(v1 已证 48 发散/32 无效) |
| 位置环 | Ki | 0 | 红线不动 | — |
| 位置环 | Kd | **550** | 起点(全史最优核) | [400, 700],步 ±150 粗→±75 细(v1 已证 400 更差/700 无效) |
| 位置环 | D 滤波 α | **0.4** | 起点(τ≈3.9ms) | {0.4, 0.2} 二档(0.2 对乒乓已证伪,仅治高频碎抖) |
| 位置环 | gyro_kd | 0 | 死字段(v1 F21 勘误:`PID_GYRO_ENABLE` 恒 0 未编译) | 后手:启用须 `#define PID_GYRO_ENABLE 1` + 单变量专测轮 |
| 位置环 | corr slew | 999(关) | v1 F22c 两轮证伪关断 | 复用=改回 15 |
| 速度环 | Kp/Ki/Kd | 2.0/14.0/2.0 | 不动 | 仅当速度跟踪本身异常才动 |
| 占空比 | START_L/R | 870/990 | 🔒**用户冻结** | 解冻须用户拍板 |
| 占空比 | HOLD_L/R | 850/940 | 🔒**用户冻结**(纯电池唯一过段验证档) | 同上 |
| 占空比 | S_MODE_HOLD | 730/760 | 🔒**用户冻结**(19:49 S 配方组成部分) | 同上 |
| 占空比 | boost 发车帽 | 500ms/100 | 🔒**用户冻结** | 同上 |
| 刻度 | 里程 | ≈2.4cm/cnt | ⚠硬件改动后须复验 | R0 手推 1m 读 el/er |
| 保留 | F22a/b、F18a、F16ab、SEG 选择器、风机 G 链、19:49 S 配方 | — | 全部现役不动 | — |

---

## 调参路线图(通过新实测定正确区间)

### R0 — 上车前标定(共 ~5 分钟,不烧录不改码)
1. **手测电池电压并报数**(硬门,v1 连欠 6 轮;亏电下一切数据=污染样本)。
2. **手推 1m 验里程刻度**:直线手推车 1m,读串口 el/er 计数。期望 ≈42cnt(2.4cm/cnt);偏差 >15% 则里程门(T2/T3/U3/RD/FINISH)全体重标,先报我。
3. (可选 10s)上图前 K1 静置:S 传感器全帧、bv 读数、fn 风机起停沿,各看一眼。

### R1 — 新硬件基准轮(F23 `3a06988` 原样烧录,段1,零改动)
**目的**:单参考轮拿到新硬件/新地面下全部行为签名的方向读数。任何预防性改参都会污染归因——R1 不改任何东西。

**判读决策树(对照 v1 11:00 轮 = 旧地面最后一轮)**:

| R1 签名 | 判读 | R2 动作 |
|---|---|---|
| 发车首 el/er 动帧 <300ms | 发车正常,新硬件摩擦未恶化 | — |
| 发车堵转 >1s(sent 爬至 1200+ 而 el/er≈0) | 先查电压;满电仍堵=新硬件静摩擦↑ | 占空比冻结 → 仅报告用户议解冻,**不擅动** |
| 直线 pos 摆幅 ≤15~35(19:49 量级) | 新地面红利,直线收敛 | 直接进 U 弯/连段验收 |
| 等幅满幅 0↔60 对穿(11:00 同款) | 结构性极限环残留(v1 已证对 Kp/Kd/α/slew 四旋钮免疫) | **不动增益**;根治=质心 ADC 内插(夜间项目,需拍板) |
| 逐摆增长发散(15→30→60 爬升) | 植物增益↑(新硬件抓地/几何变化) | Kp 40→34 |
| 回中迟缓/大弧蛇行不收 | 增益不足(新硬件摩擦↑/抓地↓) | Kp 40→46 |
| 贴线高频碎抖(±1 格高频) | D 过强或噪声 | Kd 550→475 **或** α 0.4→0.2(二选一,单变量) |
| el/er 双冻结 + pid 非零 + yw 恒值 | 机械托底残留 | 标记发生位置报用户(地面优化未覆盖点),非参数问题 |
| 单轮负速/el 倒退 | 编码器通道/插头问题(v1 鉴别树) | 停车查硬件,非参数问题 |
| U 弯 u=1 自停,yw≈176 | 过段 | 连过 2 轮 → TEST_SEGMENT=2 |

**区间逼近法**(用户要求"波动接近"):每轮单变量,按上表方向移动;同向连续 2 轮改善则继续,首次变差即对分回退(如 Kp 40→34 差 → 试 37)。每轮结论回写本日志。

---

## 2026-06-07 13:57 - 开档轮:时代分界 + R1 初始参数定版(无代码改动)

**用户原话(本轮全部)**:
> "当前的小车硬件结构和地面平整度进行了一定的修改优化,对PID等参数值会有影响,需要从新的数据获得正确值区间进行波动接近,之前的PID之类参数值可能无效了,现在给车接近合理值的初始测试的PID参数,我们需要通过新实测确定正确区间. 记得同步记录日志,日志要包含串口完整调试信息,代码改动,以及串口信息对应的代码版本,以及当前代码的表现情况噢,最后还有小车测试的各种硬件条件以及具体遇到的困难以及我给你发的所有信息。然后我要求每次改完代码都push github"
> "这里的日志需要更新嘛?现在的日志会不会太长,从新的开始写会更好嘛?"
> "这里的占空比先不改"

**代码改动**:无(F23 `3a06988` 即初始参数载体,Kp40/Kd550/Ki0/α0.4 已在码内)。本轮仓库改动=日志轮换(v1 封档至 archives/ + 本文件开档)。
**版本映射**:R1 待烧录固件=**3a06988**(F23);串口归档命名沿用 `serial_raw_YYYYMMDD_HHMM_<标签>.txt`。
**表现**:未跑车,待 R1。
**硬件条件**:硬件结构+地面平整度已修改优化(具体项待用户描述补录);电池电压未测(R0 硬门);风机/赛道布局沿用与否待确认。
**困难**:①硬件具体改动项尚未获得清单,影响评估只能按域划分;②旧里程刻度/托底/褶皱类结论真伪未知,全压在 R0/R1 实测上。

---
