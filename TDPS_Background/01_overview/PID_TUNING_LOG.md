# PID 调参日志 v2 —— 新硬件时代(2026-06-07 中午起)

> **开档**:2026-06-07 13:57。
> **旧日志封档**:`archives/PID_TUNING_LOG_v1_F1-F23_20260605-0607.md`(7340 行,覆盖 2026-06-05 ~ 06-07 上午,F1~F23 全部轮次)。
> **封档原因**:①硬件结构 + 地面平整度修改优化,旧标定的物理前提已变,旧结论整体降级为"先验"(引用须注明 v1);②7340 行已超出单文件可维护范围,自然断代点轮换。
> **历史串口归档**(`serial_raw_*.txt`)仍在本目录,不随封档移动,v1 内引用路径不变。

---

## 时代分界声明(2026-06-07)

- **硬件结构**:进行了修改优化(⚠具体改动项待用户补充:配重/传感器高度/轮系/电池座/其他——记入下方 R1 条目)。
- **地面**:平整度优化。v1 旧账中两类地形病的前提可能已消失:
  - 机械托底(双轮 el/er 冻结 + yw 恒值,v1 01:50/10:59 两次复发);
  - 赛道褶皱处 START 拉不动(v1 D1 把 START 820/940→870/990 的原始动机)。
- **影响评估**:
  - 摩擦域参数(START/HOLD/boost 帽)——标定前提直接变化,旧值存疑;
  - 回路域参数(Kp/Kd/α)——植物增益(抓地/惯量/几何)可能平移,全史最优核仍是最佳先验;
  - 里程刻度(2.4cm/cnt)——若轮系/编码器被动过则失效,**须 R0 复验**。

## 用户红线(2026-06-07,本时代有效)

1. **占空比类参数先不改**(用户原话"这里的占空比先不改"):`START 870/990`、`HOLD 850/940`、`S_MODE_HOLD 730/760`、boost 发车帽——全部冻结,解冻须用户拍板。
2. 每轮测试必录日志:串口全量 + 代码改动 + 串口对应代码版本 + 表现判读 + 硬件条件 + 困难 + 用户全部原话(沿用 v1 纪律)。
3. **每次改完代码都 commit + push GitHub**(用户本日重申)。
4. 继承 v1 红线:Ki=0 不动;单变量轮次纪律;手测电池电压每轮硬性(v1 连欠 6 轮的教训)。

---

## 继承基线 = F23(commit `3a06988`,新时代 R1 固件,待烧录)

F23 = 全史复盘回滚轮:控制核回到 03:30 史上最远 + 19:49 蛇形①全通 + 06-05 直线判定通过的同一组参数。**F23 落码后尚未跑过车**——R1 既是新硬件基准轮,也是 F23 的首验轮。

| 域 | 参数 | R1 值 | 状态 | 新实测扫描区间 |
|---|---|---|---|---|
| 位置环 | Kp | **40** | 起点(全史最优核) | [32, 48],步 ±6(v1 已证 48 发散/32 无效) |
| 位置环 | Ki | 0 | 红线不动 | — |
| 位置环 | Kd | **550** | 起点(全史最优核) | [400, 700],步 ±150 粗→±75 细(v1 已证 400 更差/700 无效) |
| 位置环 | D 滤波 α | **0.4** | 起点(τ≈3.9ms) | {0.4, 0.2} 二档(0.2 对乒乓已证伪,仅治高频碎抖) |
| 位置环 | gyro_kd | 0 | 死字段(v1 F21 勘误:`PID_GYRO_ENABLE` 恒 0 未编译) | 后手:启用须 `#define PID_GYRO_ENABLE 1` + 单变量专测轮 |
| 位置环 | corr slew | 999(关) | v1 F22c 两轮证伪关断 | 复用=改回 15 |
| 速度环 | Kp/Ki/Kd | 2.0/14.0/2.0 | 不动 | 仅当速度跟踪本身异常才动 |
| 占空比 | START_L/R | 870/990 | 🔒**用户冻结** | 解冻须用户拍板 |
| 占空比 | HOLD_L/R | 850/940 | 🔒**用户冻结**(纯电池唯一过段验证档) | 同上 |
| 占空比 | S_MODE_HOLD | 730/760 | 🔒**用户冻结**(19:49 S 配方组成部分) | 同上 |
| 占空比 | boost 发车帽 | 500ms/100 | 🔒**用户冻结** | 同上 |
| 刻度 | 里程 | ≈2.4cm/cnt | ⚠硬件改动后须复验 | R0 手推 1m 读 el/er |
| 保留 | F22a/b、F18a、F16ab、SEG 选择器、风机 G 链、19:49 S 配方 | — | 全部现役不动 | — |

---

## 调参路线图(通过新实测定正确区间)

### R0 — 上车前标定(共 ~5 分钟,不烧录不改码)
1. **手测电池电压并报数**(硬门,v1 连欠 6 轮;亏电下一切数据=污染样本)。
2. **手推 1m 验里程刻度**:直线手推车 1m,读串口 el/er 计数。期望 ≈42cnt(2.4cm/cnt);偏差 >15% 则里程门(T2/T3/U3/RD/FINISH)全体重标,先报我。
3. (可选 10s)上图前 K1 静置:S 传感器全帧、bv 读数、fn 风机起停沿,各看一眼。

### R1 — 新硬件基准轮(F23 `3a06988` 原样烧录,段1,零改动)
**目的**:单参考轮拿到新硬件/新地面下全部行为签名的方向读数。任何预防性改参都会污染归因——R1 不改任何东西。

**判读决策树(对照 v1 11:00 轮 = 旧地面最后一轮)**:

| R1 签名 | 判读 | R2 动作 |
|---|---|---|
| 发车首 el/er 动帧 <300ms | 发车正常,新硬件摩擦未恶化 | — |
| 发车堵转 >1s(sent 爬至 1200+ 而 el/er≈0) | 先查电压;满电仍堵=新硬件静摩擦↑ | 占空比冻结 → 仅报告用户议解冻,**不擅动** |
| 直线 pos 摆幅 ≤15~35(19:49 量级) | 新地面红利,直线收敛 | 直接进 U 弯/连段验收 |
| 等幅满幅 0↔60 对穿(11:00 同款) | 结构性极限环残留(v1 已证对 Kp/Kd/α/slew 四旋钮免疫) | **不动增益**;根治=质心 ADC 内插(夜间项目,需拍板) |
| 逐摆增长发散(15→30→60 爬升) | 植物增益↑(新硬件抓地/几何变化) | Kp 40→34 |
| 回中迟缓/大弧蛇行不收 | 增益不足(新硬件摩擦↑/抓地↓) | Kp 40→46 |
| 贴线高频碎抖(±1 格高频) | D 过强或噪声 | Kd 550→475 **或** α 0.4→0.2(二选一,单变量) |
| el/er 双冻结 + pid 非零 + yw 恒值 | 机械托底残留 | 标记发生位置报用户(地面优化未覆盖点),非参数问题 |
| 单轮负速/el 倒退 | 编码器通道/插头问题(v1 鉴别树) | 停车查硬件,非参数问题 |
| U 弯 u=1 自停,yw≈176 | 过段 | 连过 2 轮 → TEST_SEGMENT=2 |

**区间逼近法**(用户要求"波动接近"):每轮单变量,按上表方向移动;同向连续 2 轮改善则继续,首次变差即对分回退(如 Kp 40→34 差 → 试 37)。每轮结论回写本日志。

---

## 2026-06-07 13:57 - 开档轮:时代分界 + R1 初始参数定版(无代码改动)

**用户原话(本轮全部)**:
> "当前的小车硬件结构和地面平整度进行了一定的修改优化,对PID等参数值会有影响,需要从新的数据获得正确值区间进行波动接近,之前的PID之类参数值可能无效了,现在给车接近合理值的初始测试的PID参数,我们需要通过新实测确定正确区间. 记得同步记录日志,日志要包含串口完整调试信息,代码改动,以及串口信息对应的代码版本,以及当前代码的表现情况噢,最后还有小车测试的各种硬件条件以及具体遇到的困难以及我给你发的所有信息。然后我要求每次改完代码都push github"
> "这里的日志需要更新嘛?现在的日志会不会太长,从新的开始写会更好嘛?"
> "这里的占空比先不改"

**代码改动**:无(F23 `3a06988` 即初始参数载体,Kp40/Kd550/Ki0/α0.4 已在码内)。本轮仓库改动=日志轮换(v1 封档至 archives/ + 本文件开档)。
**版本映射**:R1 待烧录固件=**3a06988**(F23);串口归档命名沿用 `serial_raw_YYYYMMDD_HHMM_<标签>.txt`。
**表现**:未跑车,待 R1。
**硬件条件**:硬件结构+地面平整度已修改优化(具体项待用户描述补录);电池电压未测(R0 硬门);风机/赛道布局沿用与否待确认。
**困难**:①硬件具体改动项尚未获得清单,影响评估只能按域划分;②旧里程刻度/托底/褶皱类结论真伪未知,全压在 R0/R1 实测上。

---

## 2026-06-07 14:06 - ✅R0 标定轮:电压 12.60V 满电 + 手推 1m 刻度复验通过(2.5cm/cnt,门保留);硬件改动项补录=重装整车+补焊不稳线

**用户原话(本轮全部)**:
> "1. 12.60V。 2. 14:06:19.518 el=1 er=2 …(逐帧)… 直到末尾 el=40 er=40 具体是重装了小车,焊接了不稳的线。 然后 R1 跑段 1,串口全量发我。判读决策树已入档(发车堵转/等幅乒乓/逐摆发散/回中迟缓/托底签名 → 各对应哪个旋钮),我按签名定 R2 动作。你这句话不知道什么意思?"

**串口数据(手推标定,非跑车轮)**:14:06:19.518~末尾,el/er 自 1/2 平滑爬至 **40/40**(中段抽样 3/3→6/6→9/10→13/13,全程左右差 ≤1cnt,无倒退无冻结)。
**版本映射**:手推时车上固件=用户现持版本(F22 或 F23,未影响标定——el/er 遥测两版一致);R1 须烧 **3a06988/625eaab**(两者固件等价,后者只多日志)。

### R0 裁决
1. **电压硬门 ✅**:12.60V = 3S 满电(4.20V/节)。v1 连欠 6 轮的纪律首次执行;亏电污染归零,R1 数据可全信。
2. **里程刻度 ✅(门全保留)**:1m = el 40 / er 40 cnt → **2.5cm/cnt**。对照 v1(el 44/er 40,均值 42 → ≈2.38):均值偏移 -4.8%,远低于 15% 重标阈 → **全部里程门(T2/T3/U3/RD/FINISH)不动**。
3. **意外红利:左右通道对称化**。v1 的 el/er 10% 固有差(44 vs 40)消失 → 高度怀疑旧左通道多计源自"不稳的线"(脉冲毛刺),本次补焊根除。期待 R1 同步验证:静止幻速(v1 ±3~6cps)应显著下降。
4. **编码器健康**:计数单调爬升、双通道锁步、推车方向计数为正(符号 ✓,补焊未引入反号——06-06 反号事故的复发担忧解除)。

### 留观项(不动作)
- 刻度 2.38→2.5(+5%):全部里程门以 cnt 阈不变 = 物理触发点整体**推迟 ~5%**(T3 240cnt: 566→600cm;RD 380cnt: ~903→950cm)。方向上对"缺口误开火"类早触风险更安全,对"门偏晚"类(红队 T1 旧账)再加一点偏晚。首次全程轮后按实测帧复核 RD/FINISH 两门。
- 硬件改动定性:**重装整车**(几何/配重可能微移 → 植物增益待 R1 验证)+ **补焊不稳线**(信号链可靠性↑——v1 的 05:48 左侧硬件故障、单轮负速类异常的嫌疑根源已修)。

### R1 行动单(已在路线图,此处定格)
- 烧 **625eaab**(= F23 参数:Kp40/Kd550/Ki0/α0.4,占空比冻结值),TEST_SEGMENT=1,段1 起点发车。
- 串口从上电到停车全量记录发回,一行不删。
- 跑前:风机状态照报(fn=);赛道=新整平地面。

---

## 2026-06-07 14:19~14:22 - R1 基准轮(三跑):✅发车/编码器全愈 + ❌乒乓跨硬件复现(结构性裁决第10轮应验) + 🐛新实锤=SEG1出口|yaw|无符号被反向自旋骗停

**用户原话(本轮全部)**:
> (三轮串口全量粘贴)"这是第一个 / 这是第二次 / 这是第三次,测了三次,还有,你刚刚只给了一个pid值,说明现在是共享pid嘛?需要改不?" "继续"

**串口完整归档**:`serial_raw_20260607_1419-1422_R1_F23_three_runs_pingpong.txt`(三轮全量+事件标注)。
**版本映射**:测试输入固件=**3a06988**(F23,源码等价 625eaab/5cf7995——后两个只改日志)。本条目落码=无(纯判读轮)。
**硬件条件**:电池手测 **12.60V 满电**(bv 遥测读 108~111≈11.0V,再证 bv 偏低不可信,只信手测);重装后整车+补焊线;新整平地面;风机 fn=1 全程正常起停;TEST_SEGMENT=1;练习场。

### 三轮行为速读
| 轮 | 时间 | 终态 | 里程(el/er) | 判读 |
|---|---|---|---|---|
| 1 | 14:19:45~56 (11s) | **假 u=1 自停**(SEG1 DONE 误判) | 218/195(≈5.2m,已达 U 区邻域) | 乒乓→U入口附近左甩+90°→右向自旋 280°(yw +105→-172),el 独走 er 冻=右 pivot 自旋;\|yw\|=172 过 U_TURN_YAW_LATCH 门→误判过段自停 |
| 2 | 14:21:20~31 (11s) | 375 丢线自停 | 226/252(≈6.0m 轮程) | 乒乓→25.6s jc=1(骑岔图样,左2黑+右1黑,疑似 Y1 或乒乓斜穿伪岔)→28.3s 起丢线盲走 yw 漂到 -60→侧身落线(S 全黑 7 帧,jc 误+1→2)→375 自停 |
| 3 | 14:22:20~26 (6s) | 375 丢线自停 | 129/128(≈3.2m) | 乒乓→24.4s 甩出线(pos 冻 20,S 全白)→盲走 1.6s→375 自停,停时线恰好在右缘重现(pos=60) |

### ✅过关项(新硬件三连验证)
1. **发车零堵转 ×3**:发车帧后 1~2 帧 el/er 即动(<300ms)。v1 的发车憋死(10:35/10:59)定性为**纯电池亏电问题**正式闭案——满电 12.60V 下 START 870/990 三连干净。
2. **编码器/线材全愈**:三轮 el/er 锁步(差 ≤3),无倒退、无单轮冻结、无幻速尖刺;轮3 停车后 el/er 严格冻结。补焊收益实锤。
3. **托底零复发**:三轮无"双轮冻结+pid 非零"签名——新地面整平有效。
4. F18a 去抖/375 自停/风机 G 链/锁存清零(轮间 jc/yw 全清)继续全绿。

### ❌不过关:直线乒乓跨硬件复现(决策树签名#2,结构性裁决第 10 轮应验)
- 三轮自发车 ~1s 起即满幅对穿:pos 0↔55/60,每摆甩穿整条传感器(S 全白帧频发),deep 在直线被反复点燃(coast pivot 功放级,v1 同款);轮1 甚至乒乓积累到自旋甩出。
- **对照核销**:本轮 = 03:30 史上最远轮同一控制核(Kp40/Kd550/α0.4)+ 满电 + 新平地面 + 健康编码器——**电池、地面、线材、增益四个嫌疑全部排除后,乒乓依旧**。v1 Agent③ 描述函数裁决(继电器死区+±0.5 格量化+coast bang-bang=固有极限环,增益免疫)以最干净的对照在新硬件上应验。
- 与 03:30 轮的残余差异仅剩:**HOLD 850/940 vs 当时 770/800**(继电器台阶高 ~10%/17.5%=极限环幅度放大器)与供电构型。占空比冻结中,此差异仅记录不动作。

### 🐛新实锤 bug:SEG1 出口检查器无符号(main.c:1077-1079)
`dyaw_latch = |add_angle - g_yaw_zero|` 取**绝对值**后与 U_TURN_YAW_LATCH_DEG 比较——轮1 右向自旋到 yw=-172(真 U 弯应为左转 **+176**,v1 10:21 实测)同样触发 u=1+SEG1 DONE 自停。后果:自旋失控被误报"过段成功";若发生在全程跑,SEG 选择器将带着假 u=1 进入后续段逻辑。**修复候选 R2b**:门内加符号守卫(段1 U=左转,要求 dyaw≥+170 非 |dyaw|)——逻辑修复非调参,不碰占空比/PID;落码前须用户确认段1 U 方向恒为左(练习场+正赛同向)。

### 用户问题解答(已答,存档):"只给一个 PID 值,是共享 PID 吗?要改吗?"
- 是共享,且**设计正确无须改**:位置环 PID 全车仅一个实例(g_position_pid),输入=线位置误差(pos-30),输出=单一 position_correction;遥测 `pid=a,b` 是该输出按"基速±corr"分到左右轮**之后**的两个轮命令,不是两套 PID。速度环同样单实例(控均速)。
- 左右电机的物理差异不靠双 PID 解决,靠**占空比层的不对称死区**(START 870/990、HOLD 850/940)补偿——该层已冻结。拆双 PID 治不了乒乓(病根在传感器量化+死区继电器,不在环路数目)。

### R2 分叉(等用户拍板,二选一或并行)
| 选项 | 内容 | 成本 | 依据 |
|---|---|---|---|
| **R2a 占空比解冻试验** | HOLD 850/940→770/800 单变量一轮 | 改 2 个宏,5 分钟 | 03:30"长直零卡滞"即此档;当时否决它的两个理由(亏电卡滞/褶皱托底)经 R0/R1 双双消失;描述函数预测台阶降→环幅降 | 
| **R2b ADC 内插根治** | BlackPoint_Finder 二值化→暗度加权质心(pos ±0.5格→±0.1~0.2格),连带重标 deep 阈/增益曲线/edge 阈 | 半天级工程+一轮重标定 | 10 轮证据链:增益空间已穷尽,唯一结构出路;本轮 S 遥测确认中间灰度值(1365/2730)存在且正被扔掉 |
| (R2b 附带) | SEG1 出口符号守卫 | 3 行 | 本轮轮1 实锤;bug 修复类可与任一选项同车(v1 F22a/b 先例) |
- **建议次序:先 R2a 后 R2b**——R2a 五分钟能验证"幅度放大器"假设,即使不根治也能把环幅压回可跑量级(03:30 实绩);R2b 是夜间工程,两者不互斥。
- PID 增益维持 Kp40/Kd550/Ki0/α0.4 不动(本轮判读=结构性,增益免疫证据链增至 10 轮)。

### 困难
- ①乒乓=当前唯一全程阻塞项,且已确证不可经参数消除;②轮1 假过段会污染后续全程测试(R2b 附带项待批);③jc 在乒乓体制下不可信(轮2 斜穿+侧身落线两次误增),sm/路标链在乒乓治好前禁评估;④bv 遥测与手测差 ~1.6V,赛前电压只认手测纪律固化。

---

## 2026-06-07 1X:XX - 🌿HJY 分支开张:三环重构核落码(角度环+左右独立速度环+光路穷举分区),整车换控制架构实验

**分支**:`HJY`,基底=**b536f46**(LHX/2in1-single HEAD,二合一单板现行代码)。
⚠勘误:用户指令"从 main 分支拉"——仓库**不存在 main**(本地/远程均无,origin/HEAD 指向已删除的 LHX/competition);用户随后确认"直接用开 Branch 前的这个程序(二板合一)作为 copy 基础",与实际基底一致。

**用户原话(本轮全部)**:
> "从main分支branch一条新的分支明明为HJY,现在我就硬要试试,下面是方案:任务目标 基于两轮独立编码电机+单MPU姿态模块的循迹小车,重构PID控制逻辑与差速纠偏策略,修正现有控制方案缺陷,通过分区穷举+实测标定消除两台电机机械不对称性,提升行驶稳定性。
> 一、现有方案问题分析: 当前程序采用速度环共用一套PID参数,仅依靠给左右电机设置不同PWM初始死区补偿机械差异,该方式存在原理性缺陷: 1.左右电机存在齿轮间隙、摩擦阻力、电气特性不一致等固有偏差,单纯叠加固定PWM偏置,无法配合PID积分、微分环节做动态补偿; 2.固定死区属于静态修正,PID闭环动态调节过程中,积分累积、微分预判的运算逻辑会和静态偏置相互干扰,偏差无法被彻底收敛,直行、纠偏时仍易跑偏、响应不对称。
> 二、PID架构整改要求(核心): 系统分为角度外环与速度内环,严格拆分三套独立PID参数: 1.角度环(基于MPU):整车姿态唯一,左右电机共用同一组Kp/Ki/Kd,共1套PID参数; 2.速度环(基于编码器):左右两轮为独立驱动单元,左电机、右电机各自配备独立Kp/Ki/Kd,各1套PID参数; 整体总计3套独立PID参数,实现单电机转速独立闭环控制。
> 三、循迹差速纠偏优化方案: 1.红外光路状态穷举分区:对红外循迹灯组的扫描结果做全场景穷举分类,按光路组合划分行驶状态,匹配对应差速偏移力度:多光路同时检测到黑线(如3/4/5号灯):判定为直行状态,仅做微小幅度动态修正,禁止大力度差速调节;单侧光路触发(如5/6/7号灯、6/7号灯):区分偏转程度,分别匹配小力度右转、大力度右转等差速策略;其他光路组合同理,完成左转、小幅偏移、大幅偏移等场景的区间划分,收敛差速输出区间。 2.电机差异实测标定:因两台电机机械、传动、负载特性非理想对称,放弃静态PWM死区补偿,改用实测标定+串口数据采集的方式:第一步:先划定PWM、差速输出的合理大区间,完成初始参数配置;第二步:通过串口持续采集运行数据,大量实测迭代,逐步收敛参数至最优区间;针对每一种光路组合、每一种行驶状态,分别标定左右电机对应的合理PWM输出,用动态PID闭环+实测参数,彻底抵消电机固有差异。
> 四、整体执行流程: 1.代码层面拆分PID结构体/参数,实现「角度环共用、左右速度环独立」的三套PID架构; 2.完成红外光路全场景枚举,划分行驶状态与对应差速等级,限定输出区间; 3.初步配置参数,确定PWM、差速的基础范围; 4.串口监采数据,反复实测调参,精细化匹配各场景下左右电机输出; 5.整体联调,验证直行、转向、小幅纠偏等工况的稳定性"
> "直接用开Branch前的这个程序作为copy基础去改,还是二板合一的,不要搞错了"
> "记得同步记录日志…(纪律重申)…注意保证串口返回内容能够符合更新参数的实测要求,另外把你的光路扫描分情况逻辑和速度给的大致区分说明一下,我不知道你的不同扫描情况的速度转变逻辑合不合理"

### 代码改动(本条目落码,首版 HJY 核)
| 文件 | 改动 |
|---|---|
| `User/HJY_Control.h` | 新建:主开关 `HJY_CORE_ENABLE=1`、9 态枚举、全部初始参数 #define、遥测全局声明 |
| `User/HJY_Control.c` | 新建:光路分区器(黑灯加权质心 c10 分带,2^7 全组合确定性映射)+ 角度环(自带位置式 PID,直行/全黑态锁航向)+ 左右独立速度环(复用 SpeedPID 增量式结构,各 1 实例)+ per-state PWM 前馈表(初值 0,标定回填)+ 100ms 自建轮速窗 + PWM 直驱(无死区前馈级) |
| `User/main.c` | 5 处接线:include / `HJY_Init()` / 控制 tick `HJY_Control_Update()` 替代 `PID_Control_Update()`+死区输出级整段编译旁路(`#elif !HJY_CORE_ENABLE`) / K1+K3 `HJY_ResetRun()` / 遥测行分叉(HJY 专用字段) |
| `Project.uvprojx` | User 组加入 HJY_Control.c/.h(Keil 可直接编译烧录;Makefile wildcard 自动收) |

**保留不动**:LineSensor/ABEncoder/MPU/风机 G 链/OLED/按键/375 丢线自停(main 主环)/SEG1 出口 u 锁存(|yaw|≥170 自停,⚠无符号 bug 未修,自旋也会停——HJY 分支当安全网用)/电池遥测。**LHX 核代码零删除**,`HJY_CORE_ENABLE=0` 一键还原。

### 三套 PID 初始参数(方案第一步"合理大区间")
| 环 | 参数 | 初值 | 输出域 |
|---|---|---|---|
| 角度环(MPU,1套) | Kp/Ki/Kd = 120/0/0 (cnt/s per rad) | 仅直行/全黑态生效,进入时锁存航向 | 差速钳 ±6 cnt/s |
| 左速度环 | Kp/Ki/Kd = 2.0/14.0/0 | 增量式,Δ200/步 | PWM [−400, +1800] |
| 右速度环 | Kp/Ki/Kd = 2.0/14.0/0(独立实例,标定后可分道) | 同上 | 同上 |
| 发车前馈 | last_output 种 900(双轮同值,刻意对称) | 置 0=纯积分爬升 | — |
| 轮速窗 | 100ms(旧 250ms 的 2.5 倍响应;量化 ~2.5cnt/窗) | — | — |

### 光路分区 × 速度表(回答用户"速度转变逻辑合不合理"之问,详见回复)
| 状态 | 典型光路(1=最左…7=最右) | 质心带 c10 | 基速 | 差速 | vt_L/vt_R |
|---|---|---|---|---|---|
| STRAIGHT | {4},{3,4},{4,5},{3,4,5} | [25,35] | 25 | 0+角度环(±6) | ~25/25 |
| L1 小左 | {3},{2,3,4} | [15,25) | 25 | −5 | 20/30 |
| L2 中左 | {2},{2,3} | [5,15) | 22 | −10 | 12/32 |
| L3 大左 | {1},{1,2} | [0,5) | 18 | −16 | 2/34 |
| R1 小右 | {5},{4,5,6} | (35,45] | 25 | +5 | 30/20 |
| R2 中右 | {6},{6,7} | (45,55] | 22 | +10 | 32/12 |
| R3 大右 | {7} | (55,60] | 18 | +16 | 34/2 |
| ALLBLACK | ≥6 灯黑(岔口/横线) | — | 18 | 0+角度环 | 18/18 锁航向直穿 |
| ALLWHITE | 0 灯黑(丢线) | — | 沿用上一状态目标 | — | 375 兜底停 |
- 设计要点:偏得越多→基速越降+差速越大;直行态禁大差速(只许角度环 ±6 微修)=方案"禁止大力度差速调节"条款;内轮最低 2 cnt/s 仍滚动(无 coast/pivot,绕开 LHX 极限环的继电器功放级);目标速度钳 ≥0(转向靠速度差,不反转)。
- 已知未做:带边无滞回(状态可能在带界抖动,串口看 st 翻动频率再决定加不加)/L3R3 半径未实测(U 弯能否过待验)。

### 串口标定采集口径(满足"每种光路状态分别标定左右 PWM")
`HJY st= vt=L,R v=L,R pwm=L,R ang= ye= pos= lost= yw= u= fn= bv= el= er= S=…`(0x07 帧路径/小程序面板/300ms 节流全沿用)。
- **按 st 分组**即得各状态样本集:稳态 `pwm` → 回填该状态前馈表 s_ff_l/r;`vt−v` 残差 → 调各轮 Kp/Ki;`ang/ye` → 角度环验收;`S=` 原始值 → 分区器误判审计。
- st 不会显示 8(全白帧沿用上一状态目标,丢线看 lost= 上爬);300ms 帧率=每 3 个控制窗 1 帧,首测够用,不够再提频。
**版本映射**:本条目落码后=HJY 分支首 commit **c7a5fca**;测试输入=该 commit Keil 编译产物。
**表现**:未跑车。**Keil 首编通过(用户回传,ARMCC V5.06u5):0 Error / 18 Warning,Code=22072 RO=2252 RW=452 ZI=2756**。
- main.c 8 条 #177(g_dz_hold_l/r、ApplyDeadzone、Clamp×2、g_sent_motor_l/r、ApplyMotorDutyLocal 未引用)=**HJY 门控的预期签名**——死区输出级整段被编译旁路,函数/变量成死代码。无害,留作"LHX 级确实没在跑"的自证;HJY_CORE_ENABLE=0 时自然消失。
- 其余 10 条(LineSensor #188×7、Uart_Config #177、BlackPoint #167、PID_Controller 标签)=基底既有警告,非本轮引入。

---

## 2026-06-07 16:12(数据)/16:3X(落码) - HJY1 架空轮✅核心机制全验 + HJY2 三小改(窗口 200ms/停车遥测清零/e= 误差遥测)

**用户原话(本轮全部)**:
> "为了测空转需要检测到哪记录光电才能正常转"(已答:悬空=全黑 st=7 18/18;白纸黑带=直行 25/25;黑带偏移=转向态,表格入上条回复)
> (16:12 架空串口数据全量回传,倒序 53 帧)

**串口完整归档**:`serial_raw_20260607_1612_HJY1_suspended_test.txt`(倒序保持原样+判读头注)。
**版本映射**:测试输入=**c7a5fca**(HJY1,窗口 100ms);本条目落码后=**HJY2 commit**(待编号)。
**硬件条件**:架空,白纸+黑胶带对准中间 3 灯(S=4095,4095,0,0,0,4095,4095 全程);风机 fn=1;bv≈109(手测 12.60V 档,bv 偏低旧账);手持有 0.2~0.6° 自然漂移。

### HJY1 架空验收(对照 16:0X 预告的检查单)
| 检查项 | 结果 |
|---|---|
| 分类器 | ✅ st=0 全程稳定,无带边抖动(胶带居中工况) |
| 角度环 | ✅ ye=2~6(0.2~0.6° 手持漂移)→ ang=4~12(0.4~1.2 cnt/s)→ vt 微分裂 25,24/26,23——方向(正漂→右修)、量级、钳位全对 |
| 符号链 | ✅ v 全程正数,双轮前转(重装+新核双保险通过) |
| 独立环 | ✅ pwm_L≈510~800 / pwm_R≈650~963 各自工作点,左省右费与旧核死区不对称(L850/R940)同向——**方案核心主张(双独立环吸收机械不对称)首次实证** |
| 真实轮速 | ✅ el/er 均 +8/300ms ≈26.7 cnt/s,贴目标 25/23(轻微超调,架空无负载正常) |
| 发车爬升 | ✅ 首帧即 v=40,40(FF900 架空过冲后回落),无堵转 |

### 发现三问题 → HJY2 落码
1. **停车遥测陈旧值(我的 bug,已修)**:StopRun 后 v=35,35 / ang=12 挂死 6s+(el/er 冻结实锤车已停)——!is_racing 分支提前 return 不再更新显示。修=停车分支清零 v/ang/ye/e + 窗口快照作废。
2. **v 量化追噪(预告风险实锤,已调)**:100ms 窗 × 44cnt/m → 每窗 2.5cnt → v 只读 10/20/30/40,PID 全程追量化噪声(pwm_L 510~800 来回打)。修=`HJY_SPEED_WIN_MS` 100→200(v 分辨率 10→5 cnt/s,响应仍快于旧核 250ms)。
3. **⚠pwm_R=863 连续 11 帧冻结(待查,加表不改逻辑)**:与 er 实际 +8/帧(26.7≈达标)矛盾——若右环真用显示值 v_R=10(e=+13),增量式 Ki14 每窗应 +182,不可能纹丝不动;最可能=300ms 遥测对 100ms 窗的相位混叠假象(真实 v_R 在窗间 20/30 翻动)。修=遥测加 `e=L,R`(速度环当窗实吃误差),下轮一锤定音:e_R 若恒 0 邻域=混叠假象结案;e_R 若大而 pwm_R 不动=真死环再查。
**HJY2 改动**:HJY_Control.h(窗口 200+e 遥测声明)/HJY_Control.c(停车清零+e 写入)/main.c(遥测行加 e=)。LHX 侧零接触。
### 下轮门禁(HJY2 烧录)
1. 架空复测 30s:v 应见 5 的倍数(分辨率提升指纹);pwm 摆幅应显著小于 510~800;盯 e= 与 pwm_R 联动裁决问题 3。
2. 架空黑带偏右 10s:st=5/6、vt 分裂方向复验(上轮没做)。
3. 通过 → 直线上路首跑(发车爬升/直行稳定/带边抖动三项,标准见 16:0X 回复)。

---

## 2026-06-07 16:27~16:30(数据)/16:5X(判读) - ✅HJY2 架空三组全验通过:跟踪均差<0.5cnt/s,863 之谜结案,准许上路

**用户原话(本轮全部)**:
> (微信文件 16.txt 回传,477 行三组架空数据,文内自标"第一组/第二组,右灯/第三组")

**串口完整归档**:`serial_raw_20260607_1627-1630_HJY2_suspended_3groups.txt`(原文件直拷,倒序)。
**版本映射**:测试输入=**4d79959**(HJY2);本条目无落码(纯判读放行轮)。
**硬件条件**:架空,白纸+黑带三组摆位(居中/偏左/偏右);fn=1;bv≈109(手测 12.60V 档)。

### 统计判读(236 帧,跑动帧 164)
| 阶段 | 帧数 | vt | v 均值 | \|e\| 均值 | pwm_L/R 均值 |
|---|---|---|---|---|---|
| 直行 | 102 | 25,24 | 25.2,24.7 | 2.5/2.1 | 641/749 |
| R1 右偏 | 31 | 30,20 | 29.5,20.5 | 4.7/5.2 | 695/680 |
| L1 左偏 | 31 | 20,30 | 21.1,29.0 | 5.2/3.8 | 590/788 |
1. **跟踪均差 <0.5 cnt/s,三组全贴目标**——独立双速度环架空判定:可用。
2. **方向链全对**:R1=左快右慢/L1=镜像;st 分布 {0:158, 1:36, 4:42} 干净(⚠16:4X 一度误报 st=6/7/8 存在——是 grep 把 `lost=66/73/89` 子串误抓,真实数据无此三态,引以为戒:st 统计必须锚定 `HJY st=`)。
3. **不对称吸收实证②**:同速下右电机多吃 ~108 PWM(直行 641 vs 749),L1 差至 198——双环各自找平,无人工死区参与。
4. **HJY1"pwm_R=863 冻结"结案**:本轮 pwm_R 最长重复 5 帧(值 800,伴 e≈0=增量式健康平衡,Δ=Ki×0=0 本该不动);=100ms 窗×300ms 遥测混叠假象,200ms 窗未复现。**关闭**。
5. **HJY2 两修复验收**:停车帧 v/pwm/e/ang 全零(陈旧值绝迹)✓;v 现细值 13/16/19/23/26/29(分辨率提升+dt 抖动)✓。
6. 发车:首窗 pwm=1100,1100(FF900+Δ200 对称钳),v 冲 52,42 后 3 窗收敛——架空无负载正常过冲。
7. 系统性短板(非 bug 记录):架空验证不了地面负载工作点,上路后 PWM 整体抬高,FF900 够不够待实测。

### 放行:直线上路首跑(门禁三项)
①发车爬升 v 0→≥20 应 ≤4 帧(pwm 钳 1800 而 v 趴 0=堵转即 K2);②直行 st 应以 0 为主偶跳 1/4(对决上午 LHX 核 pos 0↔60 满幅乒乓);③st 高频翻转而 pos 不动=带边抖,报修加滞回。

---

## 2026-06-07 16:46(事故)/17:0X(落码) - 地面首跑右甩事故定根因=对称发车前馈 → HJY3 发车包(不对称前馈 870/990 + K1 静置 1.5s + GO 脉冲)

**用户原话(本轮全部)**:
> (事故尾段串口 ~2.5s 回传)"开始给的就是3 4 5路扫到应该走直线,但可能由于硬件原因,开局直接就向右转,因为路线丢失又向左转,最终掉头停车了,你根据串口判断一下,可以考虑加一个K1按下等待稳定几秒再走,具体看你对串口返回数据的判断"

**串口完整归档**:`serial_raw_20260607_1646_HJY2_ground_run1_rightveer_crash.txt`(尾段 23 帧;发车段未采,链条由现场口述+物理推断补全)。
**版本映射**:测试输入=**4d79959**(HJY2);本条目落码后=**HJY3 commit**(待编号)。
**硬件条件**:直线段地面首跑;fn=1;bv≈109;满电档(14:0X 手测 12.60V 后跑量不大)。

### 事故链(串口+口述重建)
1. K1 → 首个速度窗(200ms)内 pwm=0 → 窗闭合瞬间增量环首算:FF900+Δ钳200 → **双轮同跳 1100,1100(对称)**;
2. 两电机不对称(架空实测同速右多吃~108PWM;LHX 旧核 START 870/**990** 同源)→ **左轮先破死区抢转,右轮未动 → 开局右甩**(口述"直接向右转"√,非硬件故障);
3. 车右甩 → 线向传感器左侧逸出 → 分类器**正确**输出 L1→L2→L3(尾帧 st=3 vt=2,34 大左修,方向无误);
4. 甩速过快已丢线(S 全白)→ 按设计沿用 L3 目标盲左旋(lost 285→357,el 近停 er 推进=左 pivot);
5. yw 44→110→178 过 170° → **SEG1 |yaw| 锁存 u=1 自停**(口述"掉头停车"√,安全网两层中 u-latch 抢在 lose_time 375 前)。
**裁决**:分类器/双速度环/盲走策略/安全网全部按设计工作;唯一病根=**发车初值对称**。"硬件原因"排除。

### 落码 HJY3(发车包,三件套)
| # | 改动 | 内容 |
|---|---|---|
| 1 | 不对称发车前馈 | `HJY_LAUNCH_FF` 900 单值 → `FF_L=870 / FF_R=990`(两电机各自实测起动邻域)。定性:这是**初始条件标定**,非稳态死区补偿——起步后仍全动态闭环,与方案"放弃静态补偿"不冲突(方案第一步本就要求"PWM 合理大区间初始配置") |
| 2 | K1 静置窗(用户提议采纳) | `HJY_LAUNCH_HOLD_MS=1500`:K1 后电机压 0 共 1.5s(人手撤离/风机起转/MPU 安定);**航向锁存与轮速窗都延后到 GO 瞬间**初始化。遥测签名=vt 0,0 + fn=1(与停车 fn=0 可辨) |
| 3 | GO 脉冲 | 静置结束一次性把 pwm 置 870/990 直驱——首窗闭合前电机就在各自起动邻域破粘,消灭"0→1100,1100 跳变"这个甩头源头 |
**改动文件**:HJY_Control.h(两 #define 换新+HOLD)/HJY_Control.c(ResetRun 播种+静置窗与 GO 脉冲逻辑)。main.c/LHX 侧零接触。

### 下轮门禁(HJY3 烧录,直线复跑)
1. K1 后应见 ~5 帧 vt=0,0 fn=1(静置窗指纹),车纹丝不动;
2. GO 后首 2 帧:pwm 应从 870/990 邻域起步(不再 1100,1100),**车头不甩**——本轮唯一验收主项;
3. 发车爬升 v 0→≥20 仍应 ≤4 帧;若 GO 后右轮仍迟滞(er 不动 >2 帧)→ FF_R 990→1050 单变量再试;
4. 直行段三项(st 以 0 为主/带边抖动/e= 稳定)沿用上轮门禁。

---

## 2026-06-07 16:58(第二跑)/17:2X(落码) - HJY3 验收:静置窗✓GO脉冲✓但右甩复发(小一号)——三环节事故链全曝光 → HJY4 三件套(爬行段/快退绕/丢线记忆防抖)

**用户原话(本轮全部)**:
> (16:58 串口含完整发车段回传)"现在虽然先打开风机再打开电机,但是还是会开局虽然扫描到3 4 5路但是向右前方走,脱线之后会左转走,只是右转的角度比前一次小一些,但最后还是几乎开局不走直线,向右转,脱线向左修回,然后几乎掉头时停车"

**串口完整归档**:`serial_raw_20260607_1658_HJY3_ground_run2_3link_chain.txt`(发车段完整,本轮判读主料)。
**版本映射**:测试输入=**5fa4bc8**(HJY3);本条目落码后=**HJY4 commit**(待编号)。
**硬件条件**:直线段;静置窗内风机先转(用户确认);bv 跑中 107~108(较上午 109~110 微降,**电池在掉电,右轮破粘阈对电压敏感——下场前建议手测/补电**)。

### HJY3 两项验收通过
- 静置窗 ✓:K1 后 5 帧 vt=0,0 fn=1,车零动作;GO 帧 pwm=**870,990** 精确(0→1100 跳变绝迹)。
- 右甩幅度缩小 ✓(用户口述"比前一次小一些";yw 首甩 -15° vs 上轮直接甩飞)——FF 不对称方向正确,但量不足。

### 三环节事故链(本轮串口把链条全照亮)
1. **右轮 990 地面仍未破粘**:GO+300ms el=1 er=0;er 首动落在 pwm_r≈1200~1400(地面负载+bv 108 比架空阈高一截)→ 左先动小右偏,线左移(pos 30→20,st=1 正确响应);
2. **Δ200/窗慢退绕**:线丢后 L3(vt=2,34),但左轮 last_output 已爬到 1131,每窗只退 200——左轮被命令 2 却以 36 实跑 ~1s(931→731→575),大左修踩不下去,车带速冲过线(yw -15→+73 左摆中越线);
3. **丢线保持方向被残影改写**:横穿瞬间右缘 1 帧黑(遥测帧间,st 2→6)→ 全白后按"最后状态"保持 **R3(34,2)反向右旋 250°**(yw +73→-181)→ \|yaw\|≥170 安全网 u=1 停("几乎掉头停车")。
**分类器瞬时判断全程无错**;错的是三个动力学/记忆环节。

### 落码 HJY4 三件套(全部 HJY_Control.c/h,不碰共享 SpeedPID 代码)
| # | 改动 | 治哪环 |
|---|---|---|
| HJY4a | **GO 后爬行段**:双轮目标压 10cnt/s,直到两轮各计满 2cnt(都破粘)或 1s 超时才放行正常巡线;爬行期角度环不锁存 | 环1:先破粘轮被闭环按在 10(不再 30+ 狂奔),呆轮积分自寻破粘点(自标定,免猜 FF_R);甩头角钳到线不出视野量级 |
| HJY4b | **目标骤降快退绕**:窗闭合时若目标一次跌 >10cnt/s 且 last_output 高于该轮起动阈,一次性压到 FF−100(左770/右890) | 环2:转向态切入立即掉速,绕过 Δ200/窗;回正常态从 770/890 重新咬合只需 1~2 窗 |
| HJY4c | **丢线记忆防抖**:瞬时分类照常驱动,但"丢线保持锚"须同态连续 25 tick(~50ms)才更新;全白时用防抖锚非瞬时残影 | 环3:横穿残影(1 帧≈2ms)永远改写不了保持方向 |

### 下轮门禁(HJY4 烧录,直线复跑)
1. 发车序列新指纹:静置 5 帧 → GO(pwm 870/990)→ **vt=10,10 爬行帧(1~3 帧)** → vt=25,24 放行;爬行期 yw 漂移应 ≤10°;
2. 若仍右偏丢线:看爬行段 el/er——若右轮 1s 超时仍 0cnt,FF_R 990→1080 单变量(地面破粘阈实测);
3. 转向态指纹(若进弯/纠偏):vt 骤降轮的 pwm 应一窗内落到 ≤890(HJY4b 指纹),不再 1131 拖 1s;
4. 直行三项沿用;**跑前手测电压报数**(bv 已降到 107~108,LHX 时代的电压纪律对 HJY 分支同样硬性)。
5. ✅电压门已过(17:3X 用户报数):**手测 12.34V**(≈4.11V/节,健康区;较 14:0X 满电 12.60 降 0.26V,与 bv 遥测 109→107 趋势吻合)——本轮免补电放行;HJY4a 爬行段电压自适应(积分爬到破粘为止),FF 不因本次掉电预调。

---
**硬件条件**:同 R0/R1(满电 12.60V/重装+补焊/新平地面);HJY 首跑建议直线段起步。
**困难**:①方案的角度环-光路状态衔接原文未细述,实现取"直行/全黑锁航向、转向态退出重锁"语义,若与 HJY 本人构想不符再改;②100ms 窗 vs 44cnt/m 编码器=±10cnt/s 离散噪声,Kd 必须 0;③无本机编译器,首编依赖用户回传报错。

---
